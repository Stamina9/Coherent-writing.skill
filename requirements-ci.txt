# Exact direct/transitive versions for the Python 3.10/3.12 CI matrix.
# Runtime users retain the compatible range in the skill's requirements.txt.
python-docx==1.2.0
lxml==6.1.1
typing_extensions==4.16.0
PyYAML==6.0.3
